import { Dimensions } from "react-native";

export const { width, height } = Dimensions.get("window");
export const SIDE_MENU_WIDTH = width * 0.8;
export const AVATAR_SECTION_HEIGHT = height * 0.25;
export const BUTTON_HEIGHT = height * 0.065;
export const offset = width * 0.1;

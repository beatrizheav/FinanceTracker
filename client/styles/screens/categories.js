import { StyleSheet } from "react-native";

export const categoriesScreen = StyleSheet.create({
  container: {
    display: "flex",
    width: "100%",
    height: "100%",
    paddingBottom: 200,
  },
  listContainer: {
    marginTop: 50,
  },
});
